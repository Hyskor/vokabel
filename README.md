# Greenwich Vocab

Lern-App für die Englisch-Vokabeln aus dem Foto (**Pick-up A · We're from Greenwich**).

```bash
cd vokabel-app && npm install && npm run dev
```

Siehe `vokabel-app/README.md` für Details.
